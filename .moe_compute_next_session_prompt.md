Continuing moe_compute e2e bring-up on dmilinkovic/moe-compute. Read
/home/ubuntu/.claude/projects/-home-ubuntu-tt-mlir/memory/project_moe_compute_e2e_blocker.md
first — it has the full chain of 9 resolved blockers and the current one.

TL;DR of current state:
- Grid reconciliation done — `lib/OpModel/TTNN/SingletonDeviceContext.cpp`
  detects 6U Galaxy via `chipDesc.getGrid()[0] == 10` and passes
  `num_chips=32` to `configure_mock_mode` so the OpModel mock device uses
  `6u_cluster_desc.yaml` (grid 7x10) instead of `t3k_cluster_desc.yaml`
  (grid 7x8). MeshDevice::create({1, 8}, ...) opens a (1,8) submesh of
  the 32-chip mock.
- Test now compiles cleanly with `(1, 8)` mesh + `FABRIC_1D_RING` +
  `single_galaxy_1x8_torus_graph_descriptor.textproto` + `cluster_axis=1`.
  Mirrors tt-metal's `test_moe_compute_6U.py` `MOE_DEVICE_PARAMS`.
- Pipeline produces `ttnn_compiled.ttnn` flatbuffer; `skip_exec=True` in the
  test raises `TTBuilderRuntimeException: Manually skipped execution` —
  intentional, the artifact is the flatbuffer for ttrt run.

Current blocker (new — emerged after compile fix):
- `ttrt run --fabric-config fabric_1d_ring` fails with
  `DEBUG_ASSERT @ runtime/lib/ttnn/debug/debug_apis.cpp:31: expectedLayout
  == actualLayout — Layout mismatch, expected TILE, got ROW_MAJOR` on
  the first moe_compute output (per_expert_total_tokens). The flatbuffer
  declares TILE u32 DRAM interleaved (shape `(1, 1, 4, 4)` from the
  test); tt-metal returns ROW_MAJOR u32 L1 HEIGHT_SHARDED (shape
  `(num_cores=70, 4)`).
- `MoeComputeRewritePattern` only rewrites outputs 0, 3, 4 to L1 HS
  but preserves the seed's TILE encoding. The optimizer then downgrades
  output 0 (and 1, 2) to DRAM because nothing downstream consumes them.

Task: bring the compiler-declared output shapes/dtypes/layouts of
ttnn.moe_compute into agreement with tt-metal's
`MoEComputeDeviceOperation::compute_output_specs`
(third_party/tt-metal/.../moe_compute_device_operation.cpp:129–270). Tt-metal
returns:
  - Output 0 (per_expert_total_tokens): UINT32 ROW_MAJOR L1 HS — shape
    `(num_cores, align(epd*4, l1_align)/4)`.
  - Output 1 (expert_activation): UINT32 ROW_MAJOR L1 INTERLEAVED — shape
    `(1, total_tokens * align((2*epd+1)*4, l1_align)/4)`.
  - Output 2 (e_t): UINT32 ROW_MAJOR L1 INTERLEAVED — shape
    `(epd, (tokens+1) * align(4, l1_align)/4)`.
  - Output 3 (tilize_output): BFLOAT16 TILE L1 HS — shape
    `(num_cores, double_buf*token_size, hidden_size)`.
  - Output 4 (matmul_output): BFLOAT16 ROW_MAJOR L1 HS — alias of 3.
  - Output 5 (combine): from SelectiveReduceCombine.

Likely cleanest is extending `MoeComputeRewritePattern` to rebuild every
output's full type (including dtype/layout/shape), since the test's
frontend-supplied output_shapes/output_types can't know the device's
compute_with_storage_grid_size or l1_alignment. Alternative is implementing
`InferTypeOpInterface` on `MoeComputeOp` that queries the device for the
canonical specs.

After fixing types, run:
  source env/activate
  export TT_MESH_GRAPH_DESC_PATH=$(pwd)/third_party/tt-metal/src/tt-metal/tests/tt_metal/tt_fabric/custom_mesh_descriptors/single_galaxy_1x8_torus_graph_descriptor.textproto
  ttrt query --save-artifacts
  export SYSTEM_DESC_PATH=$(pwd)/ttrt-artifacts/system_desc.ttsys
  pytest test/python/golden/ttir_ops/ccl/test_moe_compute.py -x -v --save-artifacts
  ttrt run --fabric-config fabric_1d_ring "builder-artifacts/TTIRBuilder/test_moe_compute_smoke[fabric_1d_ring-1x8]/ttnn_compiled.ttnn"

Don't modify tt-metal — the user has stated it works standalone and the
remaining gap is on the tt-mlir side.

Constraints from memory: feedback_targeted_testing (only the named test;
don't expand to check-ttmlir or full regressions).
